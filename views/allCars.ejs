<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cars Index</title>
  </head>
  <body>
    <a href="/cars/new">Create new cars</a>

    <h1>All Cars</h1>
    <ul>
        <% cars.forEach(car => { %>
          <li><a href="/cars/<%= car._id %>"><%= car.make %> <%= car.model%></a></li>
        <% }); %>
      </ul>
  </body>
</html>